apiVersion: apps/v1
kind: Deployment
metadata:
  name: order-service
  labels:
    app: order-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: order-service
  template:
    metadata:
      labels:
        app: order-service
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "8081"
        prometheus.io/path: "/metrics"
    spec:
      containers:
        - name: order-service
          image: k3d-skam-registry:5111/order-service:latest
          ports:
            - containerPort: 50053
              name: grpc
            - containerPort: 8081
              name: metrics
          env:
            - name: GRPC_PORT
              value: "50053"
            - name: METRICS_PORT
              value: "8081"
            - name: DATABASE_URL
              value: "postgres://postgres:hackathon@postgres:5432/orders_db?sslmode=disable"
            - name: REDIS_ADDR
              value: "redis:6379"
            - name: PAYMENT_SERVICE_ADDR
              value: "payment-service:50055"
            - name: NOTIFICATION_SERVICE_ADDR
              value: "notification-service:50056"
          resources:
            requests:
              memory: "64Mi"
              cpu: "100m"
            limits:
              memory: "128Mi"
              cpu: "250m"
          livenessProbe:
            httpGet:
              path: /health
              port: 8081
            initialDelaySeconds: 10
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /health
              port: 8081
            initialDelaySeconds: 5
            periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: order-service
spec:
  selector:
    app: order-service
  ports:
    - port: 50053
      targetPort: 50053
      name: grpc
    - port: 8081
      targetPort: 8081
      name: metrics
  type: ClusterIP
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: order-service-hpa
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: order-service
  minReplicas: 1
  maxReplicas: 5
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 70
